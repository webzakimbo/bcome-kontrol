#!/usr/bin/env ruby
# Boot into a bcome console session
require 'bcome'
spawn_into_console = true

arguments = ARGV - [ARGV[0]]
::Bcome::Bootup.do( { breadcrumbs: ARGV[0], arguments: arguments }, spawn_into_console)
